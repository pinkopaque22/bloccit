<h4>Add a comment</h4>
 <%= form_for [post, comment] do |f| %>
   <div class="form-group">
     <%= f.label :body, class: 'sr-only' %>
     <%= f.text_field :body, class: 'form-control', placeholder: "Enter a new comment" %>
   </div>
   <%= f.submit "Submit Comment", class: 'btn btn-default pull-right' %>
 <% end %>
   <%= form_for :current_user, url: {action: "update", params: { commentable_id: :comment}} do |f| %>
   <% end %>
 
 
 
 
 
 
 
 
<%#change form url, form routes, so this form submits to post_comment or topic_comment.
 
 
 
 